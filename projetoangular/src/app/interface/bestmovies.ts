export interface BestmoviesInterface {
  id: number;
  nome: string;
  classificacao: number;
  avaliacao: number;
  data: string;
  photo: string

} // Essa interface, nada mais que é um modelo que estou exegindo as variáveis que irá rebecer e enviar informações
